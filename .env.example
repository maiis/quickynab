# Get your YNAB Personal Access Token from https://app.ynab.com/settings/developer
YNAB_ACCESS_TOKEN=your_token_here

# CLI-only: Set default budget and account IDs to skip CLI prompts
# (Web app ignores these - users select budget/account in the UI)
# You can get these IDs by running: ynab-cli init
# YNAB_BUDGET_ID=
# YNAB_ACCOUNT_ID=

# Server Configuration (optional)
# PORT=3000
# NODE_ENV=production

# CORS Configuration (optional, comma-separated list)
# ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com
